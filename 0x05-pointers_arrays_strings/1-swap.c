#inlcude "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: first integer
 * @b: second integer
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int c;
	int *ptr_c = &c
	int *ptr_a = &a;
	int *ptr_b = &b;

	ptr_c = *ptr_b;
	ptr_b = *ptr_a;
	ptr_a = ptr_c;
}
