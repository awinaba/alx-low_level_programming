#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: first integer
 * @b: second integer
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int c;
	int *ptr_c = &c
	int *ptr_a = &a;
	int *ptr_b = &b;

	c = *b;
	b = *a;
	a = *c;
}
